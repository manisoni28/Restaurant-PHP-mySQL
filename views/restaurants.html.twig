<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/bootstrap.min.css" type="text/css">
        <link rel="stylesheet" href="/css/styles.css" type="text/css">
        <script src="/js/jquery-3.1.0.min.js" type="text/javascript"></script>
        <script src="/js/bootstrap.min.js" type="text/javascript"></script>
        <script src="/js/scripts.js" type="text/javascript"></script>
        <title>All You *CAN* Eat!</title>
    </head>
    <body>
        <div class="container">
            <div class="jumbotron">
                <h1><a href="/">The Restaurant Guide</a></h1>
            </div>
            <div class="row">
                <div class="col-md-4"></div>
                <div class="col-md-4 app-menu">
                    <ul class="list-group">
                        <li class="list-group-item active"><h4><span id="hide-flag">{{cuisine.getName}}</span> Restaurants</h4></li>
                        {% if restaurants is not empty %}
                            {% for restaurant in restaurants %}
                                <li class="list-group-item"><a href="/restaurants/{{restaurant.getRestaurantProperty('id')}}">{{restaurant.getRestaurantProperty('name')}}</a></li>
                            {% endfor %}
                        {% endif %}
                    </ul>
                    <div id="new-restaurant-form">
                        <h4>Add a New {{cuisine.getName}} Restaurant:</h4>
                        <form class="form-group" action="/restaurants" method="post">
                            <input type="text" name="restaurant_name" placeholder="Enter name of restaurant" class="form-control">
                            <input type="text" name="restaurant_description" placeholder="Enter a description" class="form-control">
                            <input type="text" name="restaurant_address" placeholder="Enter restaurant address" class="form-control">
                            <input type="text" name="restaurant_phone" placeholder="Enter restaurant phone number" class="form-control">
                            <input name="cuisine_id" type="hidden" value="{{cuisine.getId}}">
                            <button type="submit" class="btn btn-submit">Add Restaurant</button>
                        </form>
                    </div>
                </div>
                <div class="col-md-4"></div>
            </div>
        </div>
    </body>
</html>
