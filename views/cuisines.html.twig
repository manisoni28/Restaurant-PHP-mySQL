<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/bootstrap.min.css" type="text/css">
        <link rel="stylesheet" href="/css/styles.css" type="text/css">
        <script src="/js/jquery-3.1.0.min.js" type="text/javascript"></script>
        <script src="/js/bootstrap.min.js" type="text/javascript"></script>
        <script src="/js/scripts.js" type="text/javascript"></script>
        <title>All You *CAN* Eat!</title>
    </head>
    <body>
        <div class="container">
            <div class="jumbotron">
                <h1>The Restaurant Guide</h1>
            </div>
            <div class="row">
                <div class="col-md-4"></div>
                <div class="col-md-4 app-menu">
                    <ul class="list-group">
                        <li class="list-group-item active"><h4>Manage Cuisines</h4></li>
                        {% if cuisines is not empty %}
                            {% for cuisine in cuisines %}
                                <li class="list-group-item">
                                    <a href="/cuisines/{{cuisine.getId}}">{{ cuisine.getName }}</a>
                                    <form action="/cuisines/{{cuisine.getId}}" method="post">
                                        <input name="_method" type="hidden" value="delete"><button type="submit">Delete This</button>
                                    </form>
                                    <form action="/cuisines/{{cuisine.getId}}" method="post">
                                        <input name="_method" type="hidden" value="patch">
                                        <input type="text" name="new_cuisine_name" placeholder="Change {{ cuisine.getName }}'s name">
                                        <button type="submit">Update</button>
                                    </form>
                                </li>
                            {% endfor %}
                        {% endif %}
                    </ul>
                    <form class="form-group" action="/cuisines" method="post">
                        <input class="form-control" type="text" name="cuisine_name" placeholder="Enter a cuisine name">
                        <button type="submit" class="btn btn-submit">Add Cuisine!</button>
                    </form>
                </div>
                <div class="col-md-4"></div>
            </div>
        </div>
    </body>
</html>
