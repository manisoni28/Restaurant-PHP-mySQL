<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/bootstrap.min.css" type="text/css">
        <link rel="stylesheet" href="/css/styles.css" type="text/css">
        <script src="/js/jquery-3.1.0.min.js" type="text/javascript"></script>
        <script src="/js/bootstrap.min.js" type="text/javascript"></script>
        <script src="/js/scripts.js" type="text/javascript"></script>
        <title>All You *CAN* Eat!</title>
    </head>
    <body>
        <div class="container">
            <div class="jumbotron">
                <h1><a href="/">The Restaurant Guide</a></h1>
            </div>
            <div class="row">
                <div class="col-md-4"></div>
                <div class="col-md-4 app-menu">
                    <ul class="list-group">
                        <li class="list-group-item active"><h4>{{restaurant.getRestaurantProperty('name')}} Reviews</h4></li>
                        {% if reviews is not empty %}
                            {% for review in reviews %}
                                <li class="list-group-item">{{review.getReviewProperty('review')}}</li>
                            {% endfor %}
                        {% endif %}
                    </ul>
                    <form class="form-group" action="/cuisines" method="post">
                        <input class="form-control" type="text" name="cuisine_name" placeholder="Enter a cuisine name">
                        <button type="submit" class="btn btn-submit">Add Cuisine!</button>
                    </form>
                </div>
                <div class="col-md-4"></div>
            </div>
        </div>
    </body>
</html>
